# NMPC 控制器详解：从物理到“预测+优化”

本文档面向**没有控制背景**的读者，结合物理方程解释 `nmpc_controller.py` 第 67–135 行在做什么：  
先回顾“系统长什么样、方程在描述什么”，再说明“控制器如何通过预测未来、最小化一个代价”来算出一组加速度指令。

---

## 一、物理背景：缆索机器人在做什么

### 1.1 系统长什么样

- **缆索机器人**：上方有一个可移动的**控制点**（代码里叫 mocap），通过缆索吊着一个**负载**（重物/钢筋等）。  
- 你只能**直接控制**控制点的运动（例如给它加速度）；负载是被缆索“拖着走”的，会像**摆**一样晃动。  
- **任务**：在水平面（XY）上，把**负载**送到目标位置；控制点要配合移动，既要把负载“拉”过去，又要尽量减小摆动。

可以抽象成：

- **控制点**：位置 $\mathbf{p} = (p_x, p_y)$，速度 $\mathbf{v}_p = (v_{px}, v_{py})$，加速度 $\mathbf{a} = (a_x, a_y)$ 由我们给定。  
- **负载**：位置 $\mathbf{q} = (q_x, q_y)$，速度 $\mathbf{v}_q = (v_{qx}, v_{qy})$，受缆索拉力和阻尼影响，不能直接给定。

### 1.2 动力学方程在描述什么（你已经看懂的部分）

代码里的 8 维状态是：

$$
\mathbf{x} = (p_x,\, p_y,\, v_{px},\, v_{py},\, q_x,\, q_y,\, v_{qx},\, v_{qy})
$$

控制是 2 维：$\mathbf{u} = (a_x,\, a_y)$。

**连续动力学** $\dot{\mathbf{x}} = f(\mathbf{x}, \mathbf{u})$ 可以分成两块理解：

1. **控制点（双积分器）**  
   - 位置变化率 = 速度：$\dot{p}_x = v_{px},\ \dot{p}_y = v_{py}$  
   - 速度变化率 = 加速度：$\dot{v}_{px} = a_x,\ \dot{v}_{py} = a_y$  
   也就是说：我们**直接指定**控制点的加速度，它按牛顿定律运动。

2. **负载（平面摆 + 阻尼）**  
   - 位置变化率 = 速度：$\dot{q}_x = v_{qx},\ \dot{q}_y = v_{qy}$  
   - 速度变化率 = 缆索产生的“回复力”减阻尼：  
     $$
     \dot{v}_{qx} = -\omega^2 (q_x - p_x) - d\, v_{qx},\quad
     \dot{v}_{qy} = -\omega^2 (q_y - p_y) - d\, v_{qy}
     $$
   - $\omega^2 = g/L$：和缆长 $L$、重力 $g$ 有关，相当于**单摆的角频率平方**；$(q - p)$ 是负载相对控制点的偏移，缆索张力近似成“弹簧”，把负载往控制点方向拉。  
   - $d$ 是阻尼系数，抑制摆动。

所以：**控制点**是我们“把手”；**负载**是被缆索和阻尼拖着走的摆，我们只能通过移动控制点间接地让它到达目标。

---

## 二、NMPC 在干什么（直觉）

- **NMPC = 非线性模型预测控制**：在每个控制步，我们**假设**未来一段时间内系统按上面的动力学演化，在这段时间里**选一整段加速度序列**，使得某个“代价”最小；然后**只执行这一段里的第一步**加速度，下一步再重新预测、重新优化。  
- 这样做的目的：**提前考虑未来**，既让负载靠近目标，又避免控制点乱冲、负载乱摆。

下面几节就是把“预测 + 选加速度序列 + 最小化代价”对应到代码里的**决策变量、约束、代价、NLP**。

---

## 三、第 67–86 行：决策变量与“参数”

### 3.1 预测时域与决策变量

- **预测时域**：未来 $N$ 个控制步（代码里 $N=20$，每步 $\Delta t = 0.1\,\mathrm{s}$，即预测 2 秒）。  
- **决策变量**就是这段预测里的**整条轨迹**：  
  - $\mathbf{X}$：$8 \times (N+1)$，每一列是某一时刻的 8 维状态（共 $N+1$ 个时刻：0, 1, …, N）。  
  - $\mathbf{U}$：$2 \times N$，每一列是某一时刻的 2 维控制（加速度），共 $N$ 段。

也就是说：**我们在优化“未来 N 步的状态序列 + N 步的加速度序列”**，让它们满足动力学，并且让某个代价最小。

### 3.2 代价权重（第 75–79 行）

- `Q_pos`：惩罚**负载**与**目标位置**的偏差，希望负载到目标。  
- `Q_swing`：惩罚**负载与控制点的偏差** $(q - p)$，即希望缆索不要拉得太斜、摆动不要太大。  
- `Q_vel`：惩罚**控制点速度**，避免控制点跑得太猛。  
- `R_acc`：惩罚**加速度**，避免控制过于剧烈。

这些权重后面会乘在对应的平方项上，形成“代价”（标量），优化器会**最小化**这个总代价。

### 3.3 参数（第 81–86 行）

- **参数**是**每次求解时已知、不参与优化**的量：  
  - `X_init`：当前观测到的 8 维状态（当前时刻的 $\mathbf{x}$）。  
  - `P_ref`：目标位置 $(x_{\mathrm{ref}}, y_{\mathrm{ref}})$（负载要去的点）。

- **初始状态约束**（第 85–86 行）：  
  $$
  \mathbf{X}_{:,0} = \mathbf{X}_{\mathrm{init}}
  $$  
  含义：预测轨迹的**第一步**必须等于当前真实状态，这样预测才从“现在”开始。

---

## 四、第 88–105 行：动力学约束 + 代价（核心）

### 4.1 离散化：从连续方程到“下一步怎么算”

连续动力学是 $\dot{\mathbf{x}} = f(\mathbf{x}, \mathbf{u})$。要在计算机里做预测，必须把它变成**离散**的：  
“若当前是 $\mathbf{X}_k$、控制是 $\mathbf{U}_k$，则下一步 $\mathbf{X}_{k+1}$ 是多少？”

代码用 **四阶 Runge-Kutta（RK4）** 做数值积分，把 $\Delta t$ 内的演化算出来，得到  
$$
\mathbf{X}_{k+1} = \mathbf{X}_k + \frac{\Delta t}{6}(k_1 + 2k_2 + 2k_3 + k_4),
$$  
其中 $k_1, k_2, k_3, k_4$ 是用 $f$ 在不同中间点和时刻算出来的斜率（见代码 91–95 行）。  
这样，**只要给定 $\mathbf{X}_k$ 和 $\mathbf{U}_k$，$\mathbf{X}_{k+1}$ 就被唯一确定**。

### 4.2 动力学约束（第 96 行）

对 $k = 0, 1, \ldots, N-1$，约束是：  
$$
\mathbf{X}_{:,k+1} - \mathrm{RK4}(\mathbf{X}_{:,k}, \mathbf{U}_{:,k}) = 0.
$$  
含义：**你选的轨迹必须满足离散动力学**；不能随便画一条状态轨迹，必须是由初始状态 + 控制序列“推”出来的。

### 4.3 阶段代价（第 98–102 行）

在**每一步** $k$ 上，往总代价里加四项（都是平方项，表示“越小越好”）：

1. **负载与目标距离**：$Q_{\mathrm{pos},0}(q_x - x_{\mathrm{ref}})^2 + Q_{\mathrm{pos},1}(q_y - y_{\mathrm{ref}})^2$  
   → 希望负载尽快靠近目标。

2. **摆动（负载与控制点偏差）**：$Q_{\mathrm{swing},0}(q_x - p_x)^2 + Q_{\mathrm{swing},1}(q_y - p_y)^2$  
   → 希望缆索尽量垂直、摆动小。

3. **控制点速度**：$Q_{\mathrm{vel}}(v_{px}^2 + v_{py}^2)$  
   → 希望控制点不要跑得太快。

4. **控制量（加速度）**：$R_{\mathrm{acc}}(a_x^2 + a_y^2)$  
   → 希望加速度不要太大，控制平滑。

对 $k=0,\ldots,N-1$ 都加一遍，就得到**整条轨迹的阶段代价之和**。

### 4.4 终端代价（第 104–105 行）

在**最后一个时刻** $N$ 再额外加一笔：  
同样是负载与目标的偏差的平方，但权重放大 10 倍。  
含义：**特别强调预测终点处负载要接近目标**，这样优化出来的轨迹不会“前面还行、最后一段乱跑”。

---

## 五、第 107–123 行：组装成“一个数学问题”并求解

### 5.1 非线性规划（NLP）是什么

我们把上面所有内容写成一个标准的**约束优化问题**：

- **决策变量**：把 $\mathbf{X}$ 和 $\mathbf{U}$ 拉成一个大向量 $\mathbf{z}$（先所有状态，再所有控制）。  
- **目标**：最小化总代价 $J = \sum \text{阶段代价} + \text{终端代价}$（代码里的 `cost`）。  
- **等式约束**：  
  - 初始状态：$\mathbf{X}_{:,0} = \mathbf{X}_{\mathrm{init}}$；  
  - 每一步动力学：$\mathbf{X}_{:,k+1} - \mathrm{RK4}(\mathbf{X}_{:,k}, \mathbf{U}_{:,k}) = 0$。  
  这些约束被拼成一个向量 $\mathbf{g}(\mathbf{z})$，要求 $\mathbf{g}(\mathbf{z}) = 0$。  
- **参数**：$\mathbf{p} = (\mathbf{X}_{\mathrm{init}}, P_{\mathrm{ref}})$，每次调用求解器时传入当前观测和目标位置。

这就是代码里 `nlp = {'x': z, 'f': cost, 'g': constraints, 'p': p}` 的含义：  
**在给定当前状态和目标下，找一组轨迹（状态+控制），使得代价最小且满足动力学和初始条件。**

### 5.2 求解器（IPOPT）

- **IPOPT** 是一个数值求解器，用来解这类“带约束的、目标可微的”非线性规划。  
- 代码里设置了：少打印（`print_level=0`）、最大迭代 20、容差、以及**热启动**（用上一步的解作为这一步的初始猜测，加快收敛）。

---

## 六、第 125–135 行：变量的界限与热启动

### 6.1 上下界

- **状态** $\mathbf{X}$：不设硬性界限（物理上允许的位置/速度范围很宽，不在这里卡）。  
- **控制** $\mathbf{U}$：限制在 $[-u_{\max}, u_{\max}]$（代码里 $u_{\max}=0.5\,\mathrm{m/s}^2$）。  
  这样优化器不会给出“无穷大加速度”，更符合实际执行能力。

### 6.2 热启动

- `last_sol`：保存**上一次** NMPC 求出的整条轨迹（状态+控制）。  
- 下一次求解时，用 `last_sol` 作为 IPOPT 的**初始猜测**。  
  因为相邻两帧的“最优轨迹”通常很接近，好的初值能减少迭代次数、提高实时性。

---

## 七、整体流程小结（对应代码 67–135）

| 步骤 | 代码位置 | 在做什么 |
|------|----------|----------|
| 1 | 67–70 | 定义“未来 N 步”的状态轨迹 $\mathbf{X}$ 和控制序列 $\mathbf{U}$ 为决策变量。 |
| 2 | 72–79 | 定义代价权重（目标、摆动、速度、加速度）。 |
| 3 | 81–86 | 定义参数（当前状态、目标位置）和初始状态约束：预测从当前状态开始。 |
| 4 | 88–96 | 用 RK4 把连续动力学离散化，并施加约束：轨迹必须按动力学演化。 |
| 5 | 98–105 | 在每一步加“阶段代价”，在终点加“终端代价”，得到总代价 $J$。 |
| 6 | 107–111 | 把 $\min J$、约束 $\mathbf{g}=0$、参数 $\mathbf{p}$ 组装成 NLP。 |
| 7 | 113–123 | 用 IPOPT 求解该 NLP（静默、有限迭代、热启动）。 |
| 8 | 125–135 | 给控制量加上下界，并准备用本次解做下一次的热启动。 |

**执行时**（`get_action`）：  
每次用**当前状态**和**目标位置**作为参数求解这个 NLP，从解里**只取第一段控制** $\mathbf{U}_{:,0} = (a_x, a_y)$ 作为本控制步的输出；下一步再重新测量状态、重新求解，如此往复。

---

## 八、和物理方程的关系（一句话版）

- **物理方程**（你已看懂的部分）：描述了控制点与负载**如何随时间和控制输入演化**。  
- **67–135 行**：  
  - 用这些方程（离散化后）**约束**“预测轨迹必须可行”；  
  - 用**代价函数**量化“负载到目标、摆动小、控制平滑”；  
  - 用**优化器**在满足约束的前提下，选出使代价最小的**整段未来控制**，并只执行第一步。  

这样就把“物理世界怎么动”和“我们怎么选控制”统一在一个数学问题里了。

---

## 九、IPOPT 求解该 NLP：输入、计算过程与输出

下面精确对应代码里 **`self.solver(x0=..., p=..., lbg=..., ubg=..., lbx=..., ubx=...)`** 的调用，说明：**输入是什么、IPOPT 内部怎么算、输出是什么、我们又如何从输出里取出“第一步加速度”。**

### 9.1 问题的标准形式（IPOPT 看到的）

IPOPT 求解的是形如：

$$
\begin{aligned}
\min_{\mathbf{z}} \quad & f(\mathbf{z}; \mathbf{p}) \\
\text{s.t.} \quad & \mathbf{lbg} \leq g(\mathbf{z}; \mathbf{p}) \leq \mathbf{ubg}, \\
& \mathbf{lbx} \leq \mathbf{z} \leq \mathbf{ubx}.
\end{aligned}
$$

其中：
- $\mathbf{z}$：**决策变量**（我们要优化的向量）；
- $\mathbf{p}$：**参数**（每次调用时给定，不参与优化）；
- $f$：**目标函数**（标量，要最小化）；
- $g$：**约束函数**（向量，必须落在 $[\mathbf{lbg}, \mathbf{ubg}]$ 内）；
- $\mathbf{lbx}, \mathbf{ubx}$：决策变量的**上下界**。

在我们这个 NMPC 里，约束是**等式**，所以设 $\mathbf{lbg} = \mathbf{ubg} = \mathbf{0}$，即 $g(\mathbf{z}; \mathbf{p}) = 0$。

---

### 9.2 输入一：决策变量的维数与含义（$\mathbf{z}$）

决策变量 $\mathbf{z}$ 由 CasADi 在**构造 NLP** 时就定好了（第 108 行）：

```python
'x': ca.vertcat(ca.reshape(X, -1, 1), ca.reshape(U, -1, 1))
```

即先把 $\mathbf{X}$（$8 \times (N+1)$）按列拉成一根向量，再把 $\mathbf{U}$（$2 \times N$）按列拉成一根向量，上下拼在一起：

$$
\mathbf{z} = \begin{bmatrix} \mathrm{vec}(\mathbf{X}) \\ \mathrm{vec}(\mathbf{U}) \end{bmatrix}.
$$

**维度**（代码里 $n_x=8,\, N=20,\, n_u=2$）：

- $\mathrm{vec}(\mathbf{X})$：$8 \times 21 = 168$ 维（21 个时刻，每时刻 8 个状态）；
- $\mathrm{vec}(\mathbf{U})$：$2 \times 20 = 40$ 维（20 段控制，每段 2 个加速度）；
- **总维数**：$n_{\mathrm{vars}} = 168 + 40 = 208$。

**含义**：  
$\mathbf{z}$ 的前 168 个数是“预测的整条状态轨迹”，后 40 个数是“预测的整条控制序列”。IPOPT 的任务就是在满足动力学和初始条件的前提下，找到使代价 $f(\mathbf{z})$ 最小的 $\mathbf{z}$。

---

### 9.3 输入二：参数 $\mathbf{p}$（每次调用时传入）

参数在构造时定义为（第 111 行）：

```python
'p': ca.vertcat(X_init, P_ref)
```

即 $\mathbf{p} = [\mathbf{X}_{\mathrm{init}}; P_{\mathrm{ref}}]$：

- $\mathbf{X}_{\mathrm{init}}$：8 维，当前观测状态；
- $P_{\mathrm{ref}}$：2 维，目标位置 $(x_{\mathrm{ref}}, y_{\mathrm{ref}})$。

**维度**：$\mathbf{p}$ 共 **10 维**。

**在 `get_action` 里**（第 150 行）：

```python
p_val = np.concatenate([state, target_pos])
```

这里 `state` 是长度为 8 的数组，`target_pos` 是长度为 2 的数组，拼成 `p_val`（形状 (10,)）传给求解器。  
因此：**每次调用时，输入是“当前状态 + 当前目标位置”，共 10 个数；它们进入 $f$ 和 $g$ 中所有出现 $X_{\mathrm{init}}$、$P_{\mathrm{ref}}$ 的地方，把问题实例化。**

---

### 9.4 输入三：初始猜测 $\mathbf{z}_0$（`x0`）

```python
x0_guess = np.zeros(self.lbx.shape)   # 默认 208 维全零
if self.last_sol is not None:
    x0_guess = self.last_sol         # 或上一时刻的完整解
```

- **作用**：IPOPT 是**迭代**算法，需要一个起点 $\mathbf{z}_0$。  
- **全零**：相当于“零状态、零控制”的轨迹，通常可行（满足约束）或至少可改进。  
- **热启动**：用上一帧的 `sol['x']` 作为本帧的 `x0`，因为相邻两帧的最优轨迹往往接近，能减少迭代次数、加快收敛。

---

### 9.5 输入四：约束的上下界（`lbg`, `ubg`）

代码里（第 159 行）：

```python
lbg=0, ubg=0
```

即 $\mathbf{lbg} = \mathbf{ubg} = \mathbf{0}$，表示 **$g(\mathbf{z}; \mathbf{p}) = 0$**（等式约束）。

**约束 $g$ 的组成**（第 86、96 行）：

1. **初始状态**：$\mathbf{X}_{:,0} - \mathbf{X}_{\mathrm{init}}$，8 个式子；
2. **动力学**：对 $k=0,\ldots,N-1$，$\mathbf{X}_{:,k+1} - \mathrm{RK4}(\mathbf{X}_{:,k}, \mathbf{U}_{:,k})$，每步 8 个式子，共 $20 \times 8 = 160$ 个。

所以 $g$ 的**总维数**是 $8 + 160 = 168$。  
IPOPT 会要求这 168 个数**全部等于 0**。

---

### 9.6 输入五：决策变量的上下界（`lbx`, `ubx`）

```python
lbx = [-inf, ..., -inf, -u_max, ..., -u_max]
ubx = [ inf, ...,  inf,  u_max, ...,  u_max]
```

- **前 168 维**（状态）：$-\infty$ 到 $+\infty$，即状态不设硬界；
- **后 40 维**（控制）：$-u_{\max}$ 到 $u_{\max}$（如 $\pm 0.5\,\mathrm{m/s}^2$），限制加速度在可执行范围内。

---

### 9.7 IPOPT 内部“怎么算”（简要）

IPOPT 是**内点法**求解器，大致过程可以理解为：

1. **给定**：$\mathbf{z}_0$、$\mathbf{p}$、$f$、$g$、以及 lbx/ubx、lbg/ubg。  
   CasADi 会为 $f$ 和 $g$ 生成**自动微分**，即给定 $\mathbf{z}$、$\mathbf{p}$ 可计算函数值、梯度、雅可比等。

2. **迭代**：  
   - 在当前 $\mathbf{z}$ 处计算 $f$、$g$ 及其导数；  
   - 把问题在当前点**近似**成一个二次规划（QP）或线性化子问题；  
   - 解这个子问题得到搜索方向，沿该方向更新 $\mathbf{z}$（并满足约束与边界）；  
   - 重复直到满足终止条件（梯度足够小、约束违反足够小等）。

3. **终止条件**（与代码里选项对应）：  
   - `ipopt.tol`、`ipopt.acceptable_tol`：控制“多接近最优、多满足约束”才停；  
   - `ipopt.max_iter`: 20：最多迭代 20 步就停，保证实时性。

4. **结果**：要么得到一个满足约束、且目标较小的 $\mathbf{z}^*$，要么在迭代上限或数值问题下**抛异常**（代码里用 `try/except` 捕获，返回零控制）。

**要点**：IPOPT 不关心 $f$、$g$ 是怎么来的（RK4、代价权重等），它只认“标量目标 $f$、向量约束 $g$、边界”，用导数值做迭代；CasADi 负责把符号的 $f$、$g$ 编译成可求导的数值函数，供 IPOPT 调用。

---

### 9.8 输出：`sol` 里有什么

调用成功后（第 161 行）：

```python
sol = self.solver(x0=x0_guess, p=p_val, lbg=0, ubg=0, lbx=self.lbx, ubx=self.ubx)
```

`sol` 是一个字典，常用键有：

| 键 | 含义 | 形状/类型 |
|----|------|------------|
| `sol['x']` | **最优决策变量** $\mathbf{z}^*$ | 长度为 208 的向量（NumPy/CasADi 数组） |
| `sol['f']` | 最优目标值 $f(\mathbf{z}^*)$ | 标量 |
| `sol['g']` | 约束在 $\mathbf{z}^*$ 处的值 $g(\mathbf{z}^*; \mathbf{p})$ | 长度 168，应接近 0 |
| 其他 | 如拉格朗日乘子、求解统计等 | 本代码未使用 |

**我们实际用到的只有 `sol['x']`**：前 168 维是最优状态轨迹 $\mathbf{X}^*$，后 40 维是最优控制序列 $\mathbf{U}^*$。

---

### 9.9 从输出中取出“第一步加速度”（代码 162–165 行）

我们只需要**当前控制步**要执行的加速度，即整段控制序列里的**第一段**：

```python
u_start_idx = self.nx * (self.N + 1)   # 168
u_opt = sol['x'][u_start_idx : u_start_idx + self.nu]   # 取第 168、169 两个数
return np.array(u_opt).flatten()   # 形状 (2,)，即 [a_x, a_y]
```

因此：

- **输出给环境的**：长度为 2 的数组 $[a_x,\, a_y]$，单位 $\mathrm{m/s}^2$，作为本步的 NMPC 基座控制指令。
- **若求解失败**（异常）：不更新 `last_sol`，返回 `np.zeros(2)`，即零加速度。

---

### 9.10 小结：输入–计算–输出一览

| 项目 | 内容 |
|------|------|
| **输入** | **$\mathbf{z}_0$**：208 维初始猜测（零或上一帧解）；**$\mathbf{p}$**：10 维（当前状态 8 + 目标 2）；**lbg=ubg=0**：168 个等式约束；**lbx/ubx**：208 维边界（状态无界，控制 $\pm u_{\max}$）。 |
| **计算** | IPOPT 在 CasADi 提供的 $f(\mathbf{z};\mathbf{p})$、$g(\mathbf{z};\mathbf{p})$ 及导数下，迭代求解 $\min f$ s.t. $g=0$、$\mathbf{lbx} \leq \mathbf{z} \leq \mathbf{ubx}$，最多 20 步。 |
| **输出** | **sol['x']**：208 维最优 $\mathbf{z}^*$（168 维状态轨迹 + 40 维控制序列）；**返回给调用方**：取其中第 168–169 维，即第一步控制 $[a_x,\, a_y]$。 |
