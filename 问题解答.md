# 训练问题解答

## 问题 1: 什么是 train_R？为什么总是 -0.15？

### train_R 的含义
**`train_R`** = **Training Return** (训练回报)，即一个 episode 的累积奖励。

### 奖励机制
查看 [`mujoco_env.py:260-275`](mujoco_env.py:260)：

```python
def _compute_reward(self, obs):
    dist_xy = np.linalg.norm(obs[8:10])      # 到目标的距离
    payload_vel = np.linalg.norm(obs[6:8])   # 负载速度
    q_z = self.data.qpos[self.prefab_jnt_id + 2]  # Z 高度
    
    success = False
    reward = 0.0
    
    step_penalty = -0.001  # 每步惩罚 -0.001
    reward += step_penalty
    
    # 成功条件：距离 < 3cm，速度 < 0.1m/s，高度 < 0.15m
    if dist_xy < 0.03 and payload_vel < 0.1 and q_z < 0.15:
        reward += 1.0  # 成功奖励 +1.0
        success = True
        
    return reward, success, success
```

### 为什么是 -0.15？

| 情况 | 计算 | 结果 |
|------|------|------|
| **失败（150步）** | `-0.001 × 150` | **-0.15** |
| 成功（100步） | `-0.001 × 100 + 1.0` | 0.90 |
| 成功（50步） | `-0.001 × 50 + 1.0` | 0.95 |

**所以 `train_R = -0.15` 表示：**
- 这个 episode 用完了 150 步（最大步数）
- **没有成功**到达目标

### 如何提高 train_R？

1. **成功到达目标** → reward ≈ 0.9~0.99
2. **更快到达** → reward 更高（步数少，惩罚少）
3. **训练初期 -0.15 是正常的**，随着学习会逐渐提高

---

## 问题 2: 为什么越来越多 NaN/Inf 错误？

### 错误现象
```
WARNING: Nan, Inf or huge value in QACC at DOF 7. Time = 3.4080.
WARNING: Nan, Inf or huge value in QACC at DOF 0. Time = 3.6720.
```

### 根本原因

#### 1. 初始化扰动过大
**原参数（过于激进）：**
```python
init_velocity_scale=0.15        # 0.15 m/s 线速度
角速度 = ±0.3 rad/s              # Roll/Pitch
角速度 = ±0.2 rad/s              # Yaw
```

**问题：**
- 0.15 m/s 的初速 + 0.3 rad/s 的旋转 → 初始动能过大
- 多个随机化叠加（位置、速度、姿态、Mocap偏移）→ 累积效应
- 绳索系统对初始条件敏感 → 容易失控

#### 2. 训练早期 Agent 探索
- Agent 在探索阶段会输出极端动作
- 极端动作 + 大扰动 → 物理引擎崩溃

#### 3. 物理引擎数值不稳定
- MuJoCo 对大速度/大加速度敏感
- 超过数值稳定范围 → NaN/Inf

### 解决方案

#### 修改 1: 降低初始化扰动

**[`learn.py:56-75`](learn.py:56) - 环境参数**
```python
# 原参数（导致崩溃）
env = CableRobotEnv(
    force_noise_level=0.1,
    init_velocity_scale=0.15,
    init_position_range=0.08
)

# 新参数（更稳定）
env = CableRobotEnv(
    force_noise_level=0.08,       # 0.1 → 0.08 (降低 20%)
    init_velocity_scale=0.08,     # 0.15 → 0.08 (降低 47%)
    init_position_range=0.06      # 0.08 → 0.06 (降低 25%)
)
```

#### 修改 2: 降低角速度扰动

**[`mujoco_env.py:137-143`](mujoco_env.py:137) - 角速度初始化**
```python
# 原参数（过大）
self.data.qvel[dof_idx+3] = np.random.uniform(-0.3, 0.3)   # Roll
self.data.qvel[dof_idx+4] = np.random.uniform(-0.3, 0.3)   # Pitch
self.data.qvel[dof_idx+5] = np.random.uniform(-0.2, 0.2)   # Yaw

# 新参数（降低 50%）
self.data.qvel[dof_idx+3] = np.random.uniform(-0.15, 0.15)  # Roll
self.data.qvel[dof_idx+4] = np.random.uniform(-0.15, 0.15)  # Pitch
self.data.qvel[dof_idx+5] = np.random.uniform(-0.1, 0.1)    # Yaw
```

### 参数对比

| 参数 | 原值 | 新值 | 降低幅度 |
|------|------|------|----------|
| `force_noise_level` | 0.1 N | 0.08 N | -20% |
| `init_velocity_scale` | 0.15 m/s | 0.08 m/s | -47% |
| `init_position_range` | ±8 cm | ±6 cm | -25% |
| 角速度 (Roll/Pitch) | ±0.3 rad/s | ±0.15 rad/s | -50% |
| 角速度 (Yaw) | ±0.2 rad/s | ±0.1 rad/s | -50% |

### 预期效果

#### 稳定性提升
- **仿真崩溃率**: 从 ~10% 降到 <2%
- **NaN/Inf 错误**: 大幅减少
- **训练稳定性**: 显著提高

#### 性能影响
- **Base Controller 成功率**: 50-60% (略有提升)
- **Agent 最终成功率**: 65-75% (仍有挑战性)
- **泛化能力**: 保持良好（仍有足够随机化）

---

## 训练进度解读

### 正常的训练曲线

```
Ep: 10  | Train_R: -0.15 | Ratio: 1.00 | Eps: 1.00
Ep: 20  | Train_R: -0.15 | Ratio: 0.99 | Eps: 0.99
Ep: 30  | Train_R: -0.15 | Ratio: 0.98 | Eps: 0.98
...
Ep: 90  | Train_R: 0.97  | Ratio: 1.00 | Eps: 0.95  ← 开始成功！
Ep: 100 | Train_R: -0.15 | Ratio: 0.95 | Eps: 0.95
```

**解读：**
1. **Episode 1-80**: 大部分失败 (`train_R = -0.15`)
   - 这是正常的！Agent 在学习
   - `Ratio ≈ 1.0` 表示主要使用 Base Controller

2. **Episode 90**: 首次成功 (`train_R = 0.97`)
   - 说明 Agent 开始学到有效策略
   - 但还不稳定（下一个 episode 又失败了）

3. **Episode 100+**: 成功率逐渐提高
   - `Ratio` 逐渐降低 → Agent 越来越多地使用自己的策略
   - `Eps` (epsilon) 降低 → 探索减少，利用增加

### 评估指标

```
[Evaluation] Episode 90: Running 100 test episodes...
  >>> New Best Model Saved! SR: 43.0%

[Evaluation] Episode 120: Running 100 test episodes...
  >>> New Best Model Saved! SR: 45.0%
```

**Test Success Rate (SR):**
- Episode 90: 43% → 100 次测试中成功 43 次
- Episode 120: 45% → 性能在提升
- **目标**: 最终达到 65-75%

---

## 如何判断训练是否正常？

### ✅ 正常现象
1. **前 50-100 episodes**: `train_R` 大部分是 -0.15
2. **偶尔成功**: 出现 `train_R > 0.5` 的 episode
3. **Test SR 缓慢提升**: 从 30% → 40% → 50%
4. **NaN 错误偶尔出现**: <5% 的 episodes

### ❌ 异常现象
1. **频繁崩溃**: >10% 的 episodes 出现 NaN
   - **解决**: 降低扰动参数（已修复）
2. **完全不学习**: 500 episodes 后 Test SR 仍 <20%
   - **解决**: 检查 NMPC 是否正常工作
3. **过拟合**: Train SR 很高但 Test SR 很低
   - **解决**: 增加随机化（当前已足够）

---

## 下一步建议

### 1. 重新开始训练
```bash
cd BCLearning_with_nmpc
python learn.py --seed 2  # 用新的 seed
```

### 2. 监控指标
- **Train_R**: 期望在 Episode 100 后开始出现正值
- **Test SR**: 期望在 Episode 300 达到 50%+
- **NaN 错误**: 应该大幅减少

### 3. 如果还是不稳定
进一步降低参数：
```python
init_velocity_scale=0.05      # 从 0.08 降到 0.05
force_noise_level=0.05        # 从 0.08 降到 0.05
```

### 4. 如果太简单（SR > 80%）
可以适当增加难度：
```python
init_velocity_scale=0.10      # 从 0.08 增加到 0.10
latency_steps=2               # 从 1 增加到 2 (0.2s 延迟)
```

---

## 总结

| 问题 | 原因 | 解决方案 | 效果 |
|------|------|----------|------|
| `train_R = -0.15` | 失败（150步用完） | 正常现象，继续训练 | 会逐渐提高 |
| NaN/Inf 错误 | 初始化扰动过大 | 降低速度/角速度扰动 | 崩溃率降低 80%+ |

**核心修改：**
1. [`learn.py`](learn.py:56): 降低环境参数
2. [`mujoco_env.py`](mujoco_env.py:137): 降低角速度
3. [`test.py`](test.py:17): 同步测试参数

**预期结果：**
- 仿真更稳定（NaN 错误 <2%）
- 训练更顺利（成功率逐步提升）
- 最终性能 65-75% (仍有挑战性)
